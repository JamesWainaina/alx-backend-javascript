export default function taskBlock (trueOrFalse) {
  const task = false;
  const task2 = true;

  if (trueOrFalse) {
    // Do not redeclare task and task2, but if necessary, modify the return or implementation logic here
  }

  return [task, task2];
}
